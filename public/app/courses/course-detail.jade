.container
  h1 {{course.title}}
    .row
      .col-md-6
        h3(ng-show="course.featured") featured
      .col-md-6
        .panel.panel-primary
          .panel-heading Tags
          .panel-body
            div(ng-repeat="tag in course.tags") {{tag}}
